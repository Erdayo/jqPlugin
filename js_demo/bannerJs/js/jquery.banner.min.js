;(function (a) {
    a.fn.SimpleBanner = function (n, f, c, g, o, d) {
        var m = 0;
        var r;
        var l = a(n).find("li").length;
        var q = a(n).find("li").width();
        k();
        function k() {
            switch (o) {
                case"scroll":
                    r = setInterval(j, d);
                    break;
                case"fade":
                    r = setInterval(p, d);
                    break;
                default:
                    console.log("type error!")
            }
        }

        function j() {
            m++;
            i(m)
        }

        function i(s) {
            if (s == l) {
                m = s = 0
            }
            a(n).find("ol").stop().animate({"margin-left": -s * q}, 500);
            h(m)
        }

        function p() {
            e(m);
            m++;
            h(m)
        }

        function e(s) {
            if (s == l) {
                m = s = 0;
                a(n).find("li").eq(l - 1).fadeIn()
            }
            if (s == l - 1) {
                a(n).find("ol").children().fadeIn()
            }
            a(n).find("li").eq(m).fadeOut()
        }

        function b(s) {
            a(n).find("li").eq(s).fadeIn();
            a(n).find("li").eq(s).prevAll().fadeOut();
            a(n).find("li").eq(s).nextAll().fadeIn()
        }

        function h(s) {
            if (s == l) {
                s = 0
            }
            a(f).find("a").eq(s).addClass("atv").siblings().removeClass("atv")
        }

        a(f).find("a").hover(function () {
            var s = a(this).index();
            m = s;
            clearInterval(r);
            if (o == "scroll") {
                i(s)
            }
            if (o == "fade") {
                b(s)
            }
            h(s)
        }, function () {
            k()
        });
        a(c + "," + g).hover(function () {
            clearInterval(r)
        }, function () {
            k()
        });
        a(c).on("click", function () {
            m--;
            if (m < 0) {
                m = l - 1
            }
            if (o == "scroll") {
                i(m)
            }
            if (o == "fade") {
                b(m)
            }
            h(m)
        });
        a(g).on("click", function () {
            m++;
            if (m == l) {
                m = 0
            }
            if (o == "scroll") {
                i(m)
            }
            if (o == "fade") {
                b(m)
            }
            h(m)
        })
    }
})(jQuery);