;(function (a) {
    a.fn.SimpleDrag = function (c) {
        var i = 0, o = 0, g = 0, h = 0, n = 0, d = 0, f = 0, l = 0, b = 0, k = 0;
        var j = false;
        var e = a(this).width(), m = a(this).height();
        a(c).on("touchstart mousedown", function (p) {
            p.preventDefault();
            j = true;
            f = a(c)[0].offsetLeft;
            l = a(c)[0].offsetTop;
            b = a(c)[0].offsetWidth;
            k = a(c)[0].offsetHeight;
            if (event.type == "mousedown") {
                i = p.clientX;
                h = p.clientY
            }
            if (event.type == "touchstart") {
                i = p.originalEvent.touches[0].clientX;
                h = p.originalEvent.touches[0].clientY
            }
        });
        a(c).on("touchmove mousemove", function (p) {
            if (!j) {
                return true
            }
            if (event.type == "mousemove") {
                o = p.clientX;
                n = p.clientY
            }
            if (event.type == "touchmove") {
                o = p.originalEvent.touches[0].clientX;
                n = p.originalEvent.touches[0].clientY
            }
            g = o - i + f;
            d = n - h + l;
            if (g < 0) {
                g = 0
            }
            if (g > e - b) {
                g = e - b
            }
            if (d < 0) {
                d = 0
            }
            if (d > m - k) {
                d = m - k
            }
            a(c).css({"left": g, "top": d})
        });
        a(c).on("touchend mouseup", function () {
            j = false
        })
    }
})(jQuery);